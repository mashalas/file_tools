# print_hash
¬ычисление контрольных сумм (crc32, md5, sha1 и т.д.)

```
file2date.py <object>

object - файл, каталог, маска файлов в каталоге.



```
* dst_root_dir - целевой каталог, в котором создать подкаталоги год/мес€ц/день, в который непосредственно будут помещатьс€ копируемые файлы
* src_file_or_dir - исходный файл, который нужно скопировать в ${dst_root_dir}/год/мес€ц/день или каталог, в котором выполнить подобную обработку дл€ всех файлов соответствующих маске
* mask - если вторым параметром указан каталог, а не отдельный файл, то выбирать дл€ копировани€ только соответствующие этой маске файлы
